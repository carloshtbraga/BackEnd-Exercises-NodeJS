const celsiusToFahrenheit = (value) => value * 1.8 + 32;

const farenheintToCelsius = (value )=> ( value - 32) /  1.8; 

const xablau = 'ABC'

module.exports = {
    celsiusToFahrenheit,
    farenheintToCelsius,
    xablau
}